package com.wanted.backend.domain.quiz.infrastructure.subscription;

// subscriptions.status 참조용(quiz 도메인 로컬). learning_activity의 SubscriptionStatus와 동일 값이지만
// 도메인 경계 유지를 위해 별도 정의한다.
public enum QuizSubscriptionStatus {
    ACTIVE
}
